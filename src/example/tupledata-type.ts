let skill:[string, number];
skill =['Programming', 5];
skill =['Developer', 5]

function doSomething(pair:[string, number]){
    const a= pair[0];
    const b = pair[1];
}
 doSomething(["hello", 40]);
